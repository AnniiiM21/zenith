import React from "react";import { motion } from "framer-motion";import { cn } from "../../utils/cn";interface AnimatedButtonProps {  children: any;  className?: string;  onClick?: () => void;  disabled?: boolean;  variant?: "primary" | "success" | "danger" | "shimmer";}export const AnimatedButton = ({  children,  className,  onClick,  disabled = false,  variant = "primary",}: AnimatedButtonProps) => {  const baseClasses = "relative inline-flex items-center justify-center px-6 py-3 font-medium text-white transition-all duration-300 transform rounded-lg focus:outline-none";    const variantClasses = {    primary: "bg-gradient-to-r from-blue-500 to-purple-600 hover:from-blue-600 hover:to-purple-700",    success: "bg-gradient-to-r from-green-500 to-emerald-600 hover:from-green-600 hover:to-emerald-700",    danger: "bg-gradient-to-r from-red-500 to-pink-600 hover:from-red-600 hover:to-pink-700",    shimmer: "bg-gradient-to-r from-blue-500 via-purple-500 to-pink-500 bg-[length:200%_100%] animate-shimmer"  };  return (    <motion.button      className={cn(baseClasses, variantClasses[variant], className)}      onClick={onClick}      disabled={disabled}      whileHover={{ scale: 1.05 }}      whileTap={{ scale: 0.95 }}      transition={{ type: "spring", stiffness: 400, damping: 17 }}    >      {children}    </motion.button>  );};